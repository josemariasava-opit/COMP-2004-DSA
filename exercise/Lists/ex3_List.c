/*
Exercise 3: Inserting a New Node at the Beginning

Goal: Add a new node at the beginning of the linked list.

    Write a function to insert a new node at the beginning of the list.
    Modify the main function to insert a few nodes at the beginning and print the linked list.
 */
#include <stdio.h>
#include <stdlib.h>
#include <fatal.h>

